# --- BasketIQ backend (Python 3.11+) ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
# 2.11.7+ required by supabase's `realtime` dep (and fine for fastapi/genai).
pydantic==2.11.7
pydantic-settings==2.7.1
python-dotenv==1.0.1

# Supabase (Postgres + Auth + Storage + pgvector)
# 2.31+ required: accepts the new sb_publishable_/sb_secret_ API key format
# (older versions reject non-JWT keys via a regex).
supabase==2.31.0

# Gemini via Vertex AI (google-genai SDK, service-account auth)
google-genai==1.2.0

# NOTE: playwright is intentionally NOT here — it's optional and only powers the
# Phase 9 collectors' browser fallback (collectors/requirements-collectors.txt).
