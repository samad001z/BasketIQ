"""/assistant routes — AI shopping assistant (natural language -> basket).

STUB — implemented in Phase 6 (wedge complete).
"""
from fastapi import APIRouter

router = APIRouter(prefix="/assistant", tags=["assistant"])
